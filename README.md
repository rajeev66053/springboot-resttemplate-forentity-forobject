# springboot-resttemplate-forentity-forobject-method

* In ForEntity method we will get responseEntity with headers.

* In ForObject method we directly get the object. We don't get the request headers.