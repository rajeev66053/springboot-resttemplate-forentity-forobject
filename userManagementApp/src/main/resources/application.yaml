server:
  port: 8083
  servlet:
    context-path: /springDataJpa
spring:
  application:
    name: spring-data-jpa
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
        format_sql: true
  datasource:
    url: jdbc:mysql://localhost:3306/spring-data-jpa?useSSL=false
    username: root
    password: T0kh@66053
    driver-class-name: com.mysql.cj.jdbc.Driver

logging:
  level:
    org:
      hibernate:
        type:
          descriptor:
            sql: TRACE